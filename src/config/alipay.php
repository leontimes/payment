<?php
return array (

    /*
    |--------------------------------------------------------------------------
    | 服务器异步通知页面路径
    |--------------------------------------------------------------------------
    |
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'notify_url' => '',
	'return_url' => '',
    /*
    |--------------------------------------------------------------------------
    | 页面跳转同步通知页面路径
    |--------------------------------------------------------------------------
    |
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'callback_url' => '',

    /*
    |--------------------------------------------------------------------------
    | 操作中断返回地址
    |--------------------------------------------------------------------------
    |
    | 用户付款中途退出返回商户的地址。
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'merchant_url' => '',

    /*
    |--------------------------------------------------------------------------
    | 签名方式
    |--------------------------------------------------------------------------
    |
    | 可能的值有：MD5、DSA、RSA/0001
    |
    */
    'sign_type' => 'MD5',

    /*
    |--------------------------------------------------------------------------
    | ca证书路径地址
    |--------------------------------------------------------------------------
    |
    | 用于curl中ssl校验.
    | 证书需放在 app/storage/cert/ 文件夹下
    |
    */
    'cacert' => app('path') . '/storage/cert/cacert.pem',

    /*
    |--------------------------------------------------------------------------
    | 商户的私钥
    |--------------------------------------------------------------------------
    |
    | 后缀是 .pem 的文件相对路径
    | 如果签名方式设置为“0001”时，请设置该参数
    |
    */
    'private_key_path' => app('path') . '/storage/key/private.pem',

    /*
    |--------------------------------------------------------------------------
    | 支付宝公钥
    |--------------------------------------------------------------------------
    |
    | 后缀是 .pem 的文件相对路径
    | 如果签名方式设置为“0001”时，请设置该参数
    |
    */
    'public_key_path' => app('path') . '/storage/key/public.pem',

    /*
    |--------------------------------------------------------------------------
    | 访问模式
    |--------------------------------------------------------------------------
    |
    | 根据自己的服务器是否支持ssl访问，若支持请选择https；若不支持请选择http
    |
    */
    'transport' => 'http',

    /*
    |--------------------------------------------------------------------------
    | 支付宝账号
    |--------------------------------------------------------------------------
    |
    | 邮箱
    |
    */
    'account' => '', //EMAIL

    /*
    |--------------------------------------------------------------------------
    | 合作身份者id
    |--------------------------------------------------------------------------
    |
    | 以2088开头的16位纯数字
    |
    */
    'partner' => '', //PID
    'payment_type' => 1,
    /*
    |--------------------------------------------------------------------------
    | 安全检验码
    |--------------------------------------------------------------------------
    |
    | 以数字和字母组成的32位字符
    | 如果签名方式设置为“MD5”时，请设置该参数
    |
    */
    'key' => '', //KEY

    'service_create' => 'create_direct_pay_by_user', //即时支付接口
    'service_execute' => 'create_direct_pay_by_user',
    'gateway' => 'https://mapi.alipay.com/gateway.do?',
);
